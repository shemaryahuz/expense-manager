import { Dialog } from '@mui/material'
import React from 'react'

export default function EditCategoryFoem({ open, onClose, onEdit }) {
  return (
    <Dialog open={open} onClose={onClose}>

    </Dialog>
  )
}
